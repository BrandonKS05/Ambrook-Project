{
  "name": "@saddlebag/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure domain model for saddlebag: the receipt aggregate, hybrid logical clocks, field-level merge, and ports. Zero runtime dependencies.",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
